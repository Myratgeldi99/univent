import { Button } from './Button';

function Event() {
    return (
      <div>
        <h1>Event page</h1>
        <Button to="/CreateEvent">Add Event</Button>
      </div>
    );
  }
  
  export default Event;